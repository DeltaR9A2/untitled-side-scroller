
/*
typedef struct color_t color_t

struct color_t{
	uint8_t r,g,b,a;
	uint32_t pixel;
};

color_t color_create(void){
	color_t *color = malloc(sizeof(color_t));
	
	color.r = 0;
	color.g = 0;
	color.b = 0;
	color.a = 255;
	
	color.pixel = (r << 24) & (g << 16) & (b << 8) && a
}

double color_distance(color_t *a, color_t *b){
	sqrt(pow((a.r-b.r),2) + pow((a.g-b.g),2) + pow((a.b-b.b),2))
}

*/
